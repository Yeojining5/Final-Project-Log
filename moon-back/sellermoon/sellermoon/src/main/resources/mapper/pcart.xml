<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kh.mybatis.mapper.pcartMapper">

	<resultMap type="kh.sellermoon.member.vo.CartVO" id="cartMap">
		<id property="cartNo" column="cart_no"/>
		<result property="memberNo"     column="member_no"/>
		<result property="mdNo"         column="md_no"/>
		<result property="cartQuantity" column="cart_quantity"/>
		<result property="orderType"    column="order_type"/>
	</resultMap> 

	<select id="getAllPCartList" resultType="kh.sellermoon.member.vo.CartVO" parameterType="int">
		select 'cart' as order_type from tb_cart
	</select>

	<select id="getPCart" resultType="String">
		select cart_no as cartNo from tb_cart where cart_no = 1
	</select>


	

</mapper>